<% content_for :title, "New dish" %>

<div class="md:w-2/3 w-full mx-auto p-4">
  <h1 class="text-2xl font-bold mb-6 text-red-500">Nouveau plat</h1>

  <%= render 'form', dish: @dish %>

  <div class="mt-4">
    <%= link_to "Retour à la liste", dishes_path,
          class: "inline-block bg-gray-800 text-white px-4 py-2 rounded hover:bg-gray-700 transition" %>
  </div>
</div>
